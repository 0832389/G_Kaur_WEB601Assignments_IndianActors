import { IndianActorsPipe } from './indian-actors.pipe';

describe('IndianActorsPipe', () => {
  it('create an instance', () => {
    const pipe = new IndianActorsPipe();
    expect(pipe).toBeTruthy();
  });
});
