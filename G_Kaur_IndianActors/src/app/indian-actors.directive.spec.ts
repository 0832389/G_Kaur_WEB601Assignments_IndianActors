import { IndianActorsDirective } from './indian-actors.directive';

describe('IndianActorsDirective', () => {
  it('should create an instance', () => {
    const directive = new IndianActorsDirective();
    expect(directive).toBeTruthy();
  });
});
